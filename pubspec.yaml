name: auto_route_example_app
description: A new Flutter project.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.17.3 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  auto_route: ^4.2.0
  salomon_bottom_bar: ^3.3.1
  http: ^0.13.4
  json_annotation: ^4.5.0
  google_fonts: ^3.0.1
  provider: ^6.0.3
  path_provider: ^2.0.11

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  auto_route_generator: ^4.2.0
  build_runner: ^2.1.11
  json_serializable: ^6.0.0

flutter:

  uses-material-design: true

  assets:
    - lib/assets/
